{
  "name": "second-brain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "imports": {
    "#app/*": "./src/*"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --use-system-ca --import tsx --watch src/index.ts",
    "start": "node --use-system-ca --import tsx src/index.ts",
    "mcp": "node --use-system-ca --import tsx src/mcp-server.ts",
    "check": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts scripts/**/*.test.ts",
    "validate:schema": "node scripts/validate-db-schema.mjs",
    "verify": "npm run check && npm run test && npm run validate:schema"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "fastify": "^5.6.1",
    "jose": "^6.1.0",
    "pg": "^8.16.3",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "@types/pg": "^8.15.5",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
